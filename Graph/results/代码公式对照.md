# 代码 ↔ 方法定稿公式 对照表 (Code–formula alignment)

逐条核对方法章 3.3–3.9 在代码中的实现位置与状态。
结论：**早期 review 说“只实现了编码器、3.4/3.6/3.7 缺失”已过时；现在 3.3–3.9 全部接上且与定稿一致。**
（行号基于本分支当前 `src/`，可能随后续提交小幅漂移。）

| 方法 | 公式/组件 | 代码位置 | 状态 | 消融开关 |
|---|---|---|---|---|
| 3.3 | 关系感知图（4 关系：同说话人/回复/自环/情绪转移）+ 关系偏置注意力 | `layer.py` `ERGATLayer` / `ERGAT`（约 L531–L665）；`model.py::_encode_graph`（L315–L318） | ✔ 一致 | `use_emotion_transition`（情绪转移边） |
| 3.3 | 情绪转移边仅 warmup 后激活 + 计划采样 | `model.py::forward` `et_for_graph`（L444–L445） | ✔ | `use_emotion_transition` |
| 3.3 | 自含原因对角签名 $e^{\mathrm{self}}$ | `model.py::_self_loop_args`（L321–L329）；`method.py::build_pair_repr` | ✔ | `use_self_loop_fix` |
| 3.4 | 候选门控（软相关门 + top-M presence 预算 $|T|$） | `model.py::forward`（L466–L468，`soft_relevance_gate` / `presence_weight`） | ✔ | （预算 `top_m`） |
| 3.5 | 扰动必要性证据 $z^{\mathrm{nec}}$：原因必要性 $\Delta^u$ + 模态局部 $\Delta^{a},\Delta^{v}$（两次整段重编码）+ 锚定基线 | `model.py::_necessity_evidence`（L331–L374）；`forward`（L470–L486）；`method.py::NecessityEvidence` / `AnchoredBaseline` | ✔ 一致 | `use_necessity` |
| 3.6 | 有界位置先验 $b^{\mathrm{pos}}$ + 惩罚 $\mathcal{L}_{\mathrm{pos}}$ | `model.py::forward`（L498–L505）；`method.py::BoundedPositionalPrior` | ✔ （“去偏/特征”矛盾已消除） | `use_pos_prior` |
| 3.7 | LLM 推理学生 $z^{\mathrm{rea}}$（只从 $h^{\mathrm{pair}}$ 预测，不抄证据）| `model.py` `reasoning_student`（L488）；`method.py::ReasoningStudent` | ✔ | `use_distillation` |
| 3.7 | 可靠度($\rho$)加权分组 BCE 蒸馏损失 $\mathcal{L}_{\mathrm{dst}}$ + 离线标注存取 | `model.py::_distillation_loss`（L376–L389）；`forward`（L513–L517）；`method.py::distillation_loss` / `ReasoningAnnotationStore` | ✔ | `use_distillation` |
| 3.8 | 互补感知条件融合（$\rho$-缩放 + 置信度 $c_s$ / 冲突 conflict 门 + 一致性 boost）| `method.py::EvidenceFusion`（约 L313–L408）；`model.py::forward`（L507–L509） | ✔ 一致 | `fusion_mode`（`cond`/`gated`） |
| 3.9 | 总损失 $\mathcal{L}=\mathcal{L}_{\mathrm{cls}}+\beta\mathcal{L}_{\mathrm{score}}+\lambda_1\mathcal{L}_{\mathrm{dst}}+\lambda_2\mathcal{L}_{\mathrm{pos}}+\gamma(\mathcal{L}_{\mathrm{anc}}+\mathcal{L}_{\mathrm{cal}})$ | `model.py::forward`（L519–L526） | ✔ 一致 | — |

## 备注 / 已对齐的实现细节
- **warmup 期**仍训练学生头（`g_ev` 置零使证据不进融合，但 $z^{\mathrm{rea}}$ 仍有梯度），见 `forward` L489–L493。
- **评估协议**：默认 `eval_threshold_on=valid`（验证集选阈值、固定到测试集），消除测试集内调参泄漏；`test` 复现旧数。见 `trainer.py::final_evaluate`。
- **消融默认全开**：上表所有开关默认 `'yes'`，即默认行为与改动前**逐位一致**（已由 `tests/test_model_integration.py` 全/旧两条通路 + 逐开关前反向验证覆盖）。
