# Hello-Agents 从零重写指南（第七章核心框架 + 第八~十二章扩展模块）

> 目标：自己把 `Fucking/Hello-Agents/hello_agents` 这套框架**从下往上重写一遍**，走通「LLM → 数据契约 → Agent 基类 → 工具系统 → 多种 Agent 范式」的完整流程，再沿「一切皆 Tool」的思路把**记忆 / 上下文工程 / 通信协议 / 强化学习 / 评估**逐章扩展上去。
> 用法：照着本文一个文件一个文件敲；每写完一层就跑对应「自测脚本」，绿了再往上写。原码在 `Fucking/Hello-Agents/` 里，遇到卡壳就翻原文件对照（文中标了原码文件名与行数）。
>
> **本文结构**：第 0~9 节是**第七章核心框架**（必做，是后面所有章节的地基）；从「# 第八章」起是**扩展模块重写指南**（按需选做，对应教程 [datawhalechina/hello-agents](https://github.com/datawhalechina/hello-agents) 第 8~12 章）。
>
> | 章节 | 主题 | 对应模块 | 何时做 |
> |---|---|---|---|
> | 第七章 | 构建你的 Agent 框架 | `core/ + agents/ + tools/` | **先做，地基** |
> | 第八章 | 记忆与检索 | `memory/`（含 RAG、存储） | 想让 Agent「记得住、查得到」 |
> | 第九章 | 上下文工程 | `context/` | 想精细控制喂给 LLM 的上下文 |
> | 第十章 | 智能体通信协议 | `protocols/`（MCP/A2A/ANP） | 想接外部工具 / 多 Agent 协作 |
> | 第十一章 | Agentic-RL | `rl/` | 想训练 / 微调模型 |
> | 第十二章 | 性能评估 | `evaluation/`（BFCL/GAIA） | 想量化 Agent 能力 |
> | 第十三~十六章 | 应用实战 | 无新核心模块 | 把前面能力组装成完整应用 |

---

## 0. 这套框架的设计哲学（先记住）

- **薄抽象、纯原生**：不用 LangChain，只用 OpenAI 官方 SDK（任何 OpenAI 兼容服务都能接）。
- **四层抽象，自底向上**：
  ```
  第四层 范式与能力层  agents/* + tools/*      ← 你平时直接用的
  第三层 抽象层        core/agent.py            ← 所有 Agent 的共同基类
  第二层 数据/契约层   core/message.py + config.py
  第一层 通信层        core/llm.py + exceptions.py  ← 和大模型说话的唯一出口
  ```
- **除了 Agent，一切皆 Tool**：Memory / RAG / RL / 协议 在原框架里都被抽象成「工具」。第七章只需掌握核心：LLM、Message、Config、Agent 基类、4~5 种 Agent、计算器/搜索两个工具。

**重写顺序 = 阅读顺序 = 第一层 → 第四层**（先打地基再盖楼）。

---

## 1. 环境准备

```bash
cd ~/repos/Fucking
cp -r Hello-Agents Hello-Agents-原版参考      # 留一份只读参考，别在它上面改
mkdir my-hello-agents && cd my-hello-agents
python -m venv .venv && source .venv/bin/activate
pip install "openai>=1.0,<2" "pydantic>=2,<3" python-dotenv requests
```

建目录骨架（和原框架一致，方便对照）：
```bash
mkdir -p hello_agents/{core,agents,tools/builtin}
touch hello_agents/__init__.py
touch hello_agents/core/__init__.py
touch hello_agents/agents/__init__.py
touch hello_agents/tools/__init__.py hello_agents/tools/builtin/__init__.py
```

建 `.env`（参考原版 `Hello-Agents/.env.example`）。最少给这三个：
```
LLM_MODEL_ID=deepseek-chat          # 或 gpt-4o-mini / qwen-plus ...
LLM_API_KEY=sk-xxxxxxxx
LLM_BASE_URL=https://api.deepseek.com   # 任何 OpenAI 兼容地址
```
> 没有云端 key？可以装 [Ollama](https://ollama.com) 本地跑：`LLM_BASE_URL=http://localhost:11434/v1`、`LLM_API_KEY=ollama`、`LLM_MODEL_ID=llama3.2`。

---

## 总览：要重写的文件清单与依赖顺序

| 顺序 | 文件 | 你要实现的核心类/函数 | 依赖谁 |
|---|---|---|---|
| 1 | `core/exceptions.py` | `HelloAgentsException` 及子类 | 无 |
| 2 | `core/llm_response.py` | `LLMResponse`、`StreamStats`、`ToolCall`、`LLMToolResponse`（结构化响应对象） | dataclasses |
| 3 | `core/llm.py` | `HelloAgentsLLM`（invoke / think / stream_invoke + `invoke_response` / `invoke_with_tools` + 异步 `ainvoke`/`astream_invoke`/`ainvoke_with_tools`） | exceptions、llm_response、openai |
| 4 | `core/message.py` | `Message`（pydantic, to_dict） | pydantic |
| 5 | `core/config.py` | `Config`（from_env） | pydantic |
| 6 | `core/lifecycle.py` | `EventType`、`AgentEvent`、`ExecutionContext`、`LifecycleHook`（事件 / 执行上下文） | dataclasses、enum |
| 7 | `core/streaming.py` | `StreamEventType`、`StreamEvent`、`StreamBuffer`、`stream_to_sse`/`stream_to_json`（SSE/JSONL 流式输出） | dataclasses、json |
| 8 | `core/session_store.py` | `SessionStore`（会话持久化、配置/Schema 一致性校验） | json、pathlib |
| 9 | `core/agent.py` | `Agent(ABC)`（抽象 run + 历史管理 + 异步 `arun`/`on_event`/`emit_event`） | message、llm、config、lifecycle |
| 10 | `tools/base.py` | `Tool(ABC)`、`ToolParameter` | pydantic |
| 11 | `tools/registry.py` | `ToolRegistry`、`global_registry` | tools/base |
| 12 | `tools/builtin/calculator.py` | `CalculatorTool`、`calculate()` | tools/base |
| 13 | `tools/builtin/search_tool.py` | `SearchTool`、`search()`（可简化） | tools/base、requests |
| 14 | `agents/simple_agent.py` | `SimpleAgent` | agent、(registry) |
| 15 | `agents/react_agent.py` | `ReActAgent` ★最重点 | agent、registry |
| 16 | `agents/reflection_agent.py` | `ReflectionAgent` + `Memory` | agent |
| 17 | `agents/plan_solve_agent.py` | `PlanAndSolveAgent` + `Planner`/`Executor` | agent |
| 18 | `agents/function_call_agent.py` | `FunctionCallAgent`（进阶，可选） | agent、registry |
| 19 | `hello_agents/__init__.py` | 统一导出 | 全部 |

> 第 2、6、7、8 项（`llm_response` / `lifecycle` / `streaming` / `session_store`）是**核心层进阶能力**——它们让框架从「只能同步一问一答」升级到「带结构化响应、工具调用、异步并发、SSE 流式、会话持久化」。先把 1/3/4/5/9 这条主线跑通对话，再回头补这几块，详见后面 **「3.2 核心层进阶」**。

---

# 第一层 · 通信层

## 1.1 `core/exceptions.py`（最简单，先开胃）

整个框架统一的异常类型。通信失败、配置缺失都抛它，上层用一个 `except` 兜住。

```python
"""异常体系"""

class HelloAgentsException(Exception):
    """HelloAgents 基础异常类"""
    pass

class LLMException(HelloAgentsException): pass
class AgentException(HelloAgentsException): pass
class ConfigException(HelloAgentsException): pass
class ToolException(HelloAgentsException): pass
```
> 对照原码 `core/exceptions.py`。第七章核心其实只用到 `HelloAgentsException`，其余子类先留着占位即可。

## 1.2 `core/llm.py` — `HelloAgentsLLM`（整个框架唯一和 LLM 通信的地方）

**职责**：上层 Agent 永远不直接碰 `openai` SDK，只调这个类的三个方法。

### 必须实现的接口
```python
class HelloAgentsLLM:
    def __init__(self, model=None, api_key=None, base_url=None,
                 provider=None, temperature=0.7, max_tokens=None,
                 timeout=None, **kwargs): ...

    # 同步三件套（第七章主线，先跑通这三个）
    def invoke(self, messages: list[dict], **kwargs) -> str: ...          # 一次性拿完整回复（返回 str，ReAct/所有 agent 都依赖它）
    def think(self, messages: list[dict], temperature=None) -> Iterator[str]: ...  # 流式逐 token
    def stream_invoke(self, messages: list[dict], **kwargs) -> Iterator[str]: ...  # think 的别名

    # 进阶：结构化响应 + 工具调用（详见 3.2）
    def invoke_response(self, messages, **kwargs) -> LLMResponse: ...     # 同 invoke，但返回含 usage/latency/推理过程的对象
    def invoke_with_tools(self, messages, tools, tool_choice="auto", **kwargs) -> LLMToolResponse: ...  # Function Calling

    # 进阶：异步（详见 3.2）
    async def ainvoke(self, messages, **kwargs) -> str: ...               # invoke 的 async 孪生，同样返回 str
    async def astream_invoke(self, messages, **kwargs) -> AsyncIterator[str]: ...  # async for 逐块
    async def ainvoke_with_tools(self, messages, tools, tool_choice="auto", **kwargs) -> LLMToolResponse: ...
```

> **一个关键约束**：`invoke()` 始终返回 `str`，不要改成返回对象——整个框架的 agents（`SimpleAgent`/`ReActAgent` …）都在 `response = self.llm.invoke(...)` 后把它当字符串用（`.strip()`、正则、切片）。想要 token 统计/耗时/推理过程用新加的 `invoke_response()`（返回 `LLMResponse`），它不破坏任何现有调用。

### 实现要点（按这个思路写）
1. **「参数优先，环境变量兜底」**：`self.model = model or os.getenv("LLM_MODEL_ID")`，timeout 同理读 `LLM_TIMEOUT`（默认 60）。
2. **provider 解析**（可以先做"极简版"，跑通后再补全）：
   - 极简版：直接 `self.api_key = api_key or os.getenv("LLM_API_KEY")`、`self.base_url = base_url or os.getenv("LLM_BASE_URL")`。第七章配了 `LLM_*` 三件套就够。
   - 完整版：抄原码的 `_auto_detect_provider()`（按 `OPENAI_API_KEY/DEEPSEEK_API_KEY/...` 环境变量、key 前缀、base_url 域名自动识别 11 种厂商）+ `_resolve_credentials()`（按 provider 填默认 base_url）+ `_get_default_model()`。**建议先写极简版，理解主流程后再回来补**。
3. **校验**：`if not all([self.api_key, self.base_url]): raise HelloAgentsException("API密钥和服务地址必须被提供或在.env文件中定义。")`
4. **建客户端**：`self._client = OpenAI(api_key=..., base_url=..., timeout=...)`
5. **`invoke`**：
   ```python
   resp = self._client.chat.completions.create(
       model=self.model, messages=messages,
       temperature=kwargs.get('temperature', self.temperature),
       max_tokens=kwargs.get('max_tokens', self.max_tokens))
   return resp.choices[0].message.content
   ```
   出错包成 `HelloAgentsException`。
6. **`think`**：同样 create 但 `stream=True`，`for chunk in resp: yield chunk.choices[0].delta.content or ""`，边收边 `print`。
7. **`stream_invoke`**：`yield from self.think(messages, kwargs.get('temperature'))`。

> 对照原码 `core/llm.py`（505 行）。`think` 在 292 行、`invoke`（返回 str）在 327 行；进阶方法 `invoke_response`(352)、`invoke_with_tools`(391)、`ainvoke`(450)、`astream_invoke`(463)、`ainvoke_with_tools`(493)。**重点理解**：所有上层只依赖 `invoke/think` 这两个稳定接口，换厂商只改构造参数；进阶方法都是**加法增量**，不动原有接口。

### ✅ 自测脚本 `t1_llm.py`
```python
from dotenv import load_dotenv; load_dotenv()
from hello_agents.core.llm import HelloAgentsLLM

llm = HelloAgentsLLM()
print("invoke:", llm.invoke([{"role": "user", "content": "用一句话介绍你自己"}]))
print("stream:", "".join(llm.stream_invoke([{"role": "user", "content": "数到3"}])))
```
跑：`python t1_llm.py`，能拿到回复即第一层 OK。

---

# 第二层 · 数据 / 契约层

## 2.1 `core/message.py` — `Message`

层与层之间传递的对话消息标准结构（pydantic）。

```python
"""消息系统"""
from typing import Optional, Dict, Any, Literal
from datetime import datetime
from pydantic import BaseModel

MessageRole = Literal["user", "assistant", "system", "tool"]   # 类型即约束，写错 role 直接报错

class Message(BaseModel):
    content: str
    role: MessageRole
    timestamp: datetime = None
    metadata: Optional[Dict[str, Any]] = None

    def __init__(self, content: str, role: MessageRole, **kwargs):
        super().__init__(content=content, role=role,
                         timestamp=kwargs.get('timestamp', datetime.now()),
                         metadata=kwargs.get('metadata', {}))

    def to_dict(self) -> Dict[str, Any]:        # ← Message 和通信层之间的桥
        return {"role": self.role, "content": self.content}

    def __str__(self) -> str:
        return f"[{self.role}] {self.content}"
```
> 对照原码 `core/message.py`（33 行，几乎一字不差）。**设计意图**：全框架的"对话历史"都是 `list[Message]`；要喂给 LLM 时统一 `.to_dict()`。

## 2.2 `core/config.py` — `Config`

运行配置，可从环境变量加载。

```python
"""配置管理"""
import os
from typing import Optional, Dict, Any
from pydantic import BaseModel

class Config(BaseModel):
    default_model: str = "gpt-3.5-turbo"
    default_provider: str = "openai"
    temperature: float = 0.7
    max_tokens: Optional[int] = None
    debug: bool = False
    log_level: str = "INFO"
    max_history_length: int = 100

    @classmethod
    def from_env(cls) -> "Config":
        return cls(
            debug=os.getenv("DEBUG", "false").lower() == "true",
            log_level=os.getenv("LOG_LEVEL", "INFO"),
            temperature=float(os.getenv("TEMPERATURE", "0.7")),
            max_tokens=int(os.getenv("MAX_TOKENS")) if os.getenv("MAX_TOKENS") else None,
        )

    def to_dict(self) -> Dict[str, Any]:
        return self.dict()
```
> 对照原码 `core/config.py`（35 行）。

### ✅ 自测
```python
from hello_agents.core.message import Message
m = Message("你好", "user")
print(m.to_dict())             # {'role': 'user', 'content': '你好'}
# Message("x", "boss")        # 取消注释应当 pydantic 校验报错（role 不合法）
```

---

# 第三层 · 抽象层

## 3.1 `core/agent.py` — `Agent(ABC)`

所有 Agent 的共同基类：约定 `run()` 抽象方法 + 统一历史管理。

```python
"""Agent基类"""
import asyncio
from abc import ABC, abstractmethod
from typing import Optional
from .message import Message
from .llm import HelloAgentsLLM
from .config import Config
from .lifecycle import EventType, AgentEvent, LifecycleHook

class Agent(ABC):
    def __init__(self, name: str, llm: HelloAgentsLLM,
                 system_prompt: Optional[str] = None, config: Optional[Config] = None,
                 on_event: LifecycleHook = None):
        self.name = name
        self.llm = llm
        self.system_prompt = system_prompt
        self.config = config or Config()
        self._history: list[Message] = []
        self.on_event: LifecycleHook = on_event       # 可选异步生命周期钩子

    @abstractmethod
    def run(self, input_text: str, **kwargs) -> str:
        """运行Agent（同步）—— 每个子类必须实现自己的 run 逻辑"""
        ...

    async def arun(self, input_text: str, **kwargs) -> str:
        """异步运行：默认在线程池里跑同步 run，并在前后发 AGENT_START/FINISH/ERROR 事件。
        子类若有原生异步逻辑可重写本方法。"""
        await self.emit_event(EventType.AGENT_START, input_text=input_text)
        loop = asyncio.get_event_loop()
        try:
            result = await loop.run_in_executor(None, lambda: self.run(input_text, **kwargs))
        except Exception as e:
            await self.emit_event(EventType.AGENT_ERROR, error=str(e)); raise
        await self.emit_event(EventType.AGENT_FINISH, output=result)
        return result

    async def emit_event(self, event_type: EventType, **data):
        """设置了 on_event 才构造事件并 await 调用它，否则静默跳过。"""
        if self.on_event is None: return
        await self.on_event(AgentEvent.create(event_type, self.name, **data))

    def add_message(self, message: Message): self._history.append(message)
    def clear_history(self): self._history.clear()
    def get_history(self) -> list[Message]: return self._history.copy()

    def __str__(self): return f"Agent(name={self.name}, provider={self.llm.provider})"
    def __repr__(self): return self.__str__()
```
> 对照原码 `core/agent.py`（78 行）。**关键认知**：基类只定义"骨架"（历史怎么存、run 必须有），不同范式靠子类的 `run()` 区分。`arun/on_event/emit_event` 是**加法增量**：同步 `run` 完全不变，子类不传 `on_event` 时异步路径也零开销；想接 SSE/WebSocket 只需传一个 `async def hook(ev)`。`SimpleAgent` 已在构造里转发 `on_event`，其余 Agent 可直接 `agent.on_event = hook` 赋值。

---

## 3.2 核心层进阶（async / 工具调用 / 生命周期 / 流式 / 会话持久化）

> 这一节对应文件清单里的第 2、6、7、8 项 + `llm.py`/`agent.py` 的进阶方法。**主线（一问一答）跑通后再做这块**。设计原则只有一句：**全部加法增量，绝不改动 `invoke()->str` 和现有 agents 的行为**。

### 3.2.1 `core/llm_response.py` — 结构化响应对象

四个 `@dataclass`，纯数据、零外部依赖：

```python
@dataclass
class LLMResponse:          # invoke_response() 的返回值
    content: str            # 回复内容
    model: str              # 实际使用的模型
    usage: Dict[str, int] = field(default_factory=dict)   # prompt/completion/total tokens
    latency_ms: int = 0     # 调用耗时
    reasoning_content: Optional[str] = None               # o1/deepseek-reasoner 的推理过程
    def __str__(self): return self.content                # 向后兼容：直接打印即 content

@dataclass
class ToolCall:             # 一次工具调用：id / name / arguments(JSON 字符串)
    id: str; name: str; arguments: str

@dataclass
class LLMToolResponse:      # invoke_with_tools() 的返回值
    content: Optional[str]; tool_calls: List[ToolCall]
    model: str; usage: Dict[str, int] = field(default_factory=dict); latency_ms: int = 0

@dataclass
class StreamStats:          # 流式调用结束后的统计（llm.last_call_stats）
    model: str; usage: Dict[str, int] = field(default_factory=dict)
    latency_ms: int = 0; reasoning_content: Optional[str] = None
```
> 对照源码 `core/llm_response.py`（107 行）。**为什么 `LLMResponse.__str__` 返回 content**：这样即便有人 `print(resp)` 也不会拿到一堆字段——但注意它**不是 str**，`resp.strip()` 会报错。所以 `invoke()` 仍只返回 `str`，结构化对象交给 `invoke_response()`。

### 3.2.2 `llm.py` 的进阶方法（建在现有 `self._client` 之上）

- **`invoke_response(messages, **kwargs) -> LLMResponse`**：非流式调用，把 `usage / 耗时 / reasoning_content` 一起打包返回（用 `time.time()` 算 latency，从 `response.usage` 取 token）。
- **`invoke_with_tools(messages, tools, tool_choice="auto", **kwargs) -> LLMToolResponse`**：传 OpenAI Function Calling 的 `tools` schema，解析 `response.choices[0].message.tool_calls` 成 `List[ToolCall]`。`tool_choice` 取 `"auto"/"none"/"required"/{"type":"function",...}`。
- **`async def ainvoke(...) -> str`**：`await loop.run_in_executor(None, lambda: self.invoke(...))`——在线程池跑同步版，**返回 str（与 invoke 一致）**，不阻塞事件循环。
- **`async def astream_invoke(...) -> AsyncIterator[str]`**：vendored 版没有原生异步 adapter，这里用「后台线程跑同步流式生成器 + `queue.Queue` + `loop.call_soon_threadsafe`」桥接到 asyncio，可在 `async for chunk in llm.astream_invoke(...)` 里实时消费（异常也会透传到消费端）。
- **`async def ainvoke_with_tools(...)`**：同样 `run_in_executor` 包 `invoke_with_tools`。

### 3.2.3 `core/lifecycle.py` — 事件 / 执行上下文

- **`EventType`**（Enum）：`AGENT_START/FINISH/ERROR`、`STEP_START/FINISH`、`LLM_START/CHUNK/FINISH`、`TOOL_CALL/RESULT/ERROR`、`THINKING/REFLECTION/PLAN`。
- **`AgentEvent`**：`type / timestamp / agent_name / data`，便捷构造 `AgentEvent.create(EventType.X, agent_name, **data)`，可 `to_dict()` 序列化。
- **`LifecycleHook = Optional[Callable[[AgentEvent], Awaitable[None]]]`**：就是 `Agent.on_event` 的类型。
- **`ExecutionContext`**：`input_text / current_step / total_tokens / metadata`，带 `increment_step()/add_tokens()/set_metadata()/get_metadata()`——给多步 Agent 跨步骤传状态用。

### 3.2.4 `core/streaming.py` — SSE / JSONL 流式输出

给 Web 后端（FastAPI 等）把 Agent 事件推给前端用：

- **`StreamEvent`**：`to_sse()` 产出标准 `event: <type>\ndata: <json>\n\n`；`to_dict()` 给 JSONL。
- **`StreamBuffer(max_buffer_size=100)`**：`add/get_all/clear/filter_by_type`，自带「超容量丢最旧」的背压。
- **`async stream_to_sse(event_stream, include_types=None)` / `stream_to_json(...)`**：把一个 `AsyncIterator[StreamEvent]` 转成 SSE / JSON Lines 字符串流，可按类型过滤。

### 3.2.5 `core/session_store.py` — 会话持久化

- **`SessionStore(session_dir="memory/sessions")`**：
  - `save(agent_config, history, tool_schema_hash, read_cache, metadata, session_name=None) -> filepath`：原子写（临时文件 + `os.replace`），`history` 里的元素若有 `to_dict()` 会自动序列化。
  - `load(filepath)`、`list_sessions()`、`delete(session_name)`。
  - `check_config_consistency(...)` / `check_tool_schema_consistency(...)`：恢复会话前校验「模型/工具 schema 有没有变」，防止拿旧会话配新工具出错。

### ✅ 核心层进阶自测 `t1b_core_advanced.py`（离线可跑，无需 LLM/网络）
```python
import asyncio, tempfile
from hello_agents.core import (LLMResponse, ToolCall, LLMToolResponse,
    EventType, ExecutionContext, StreamEvent, StreamEventType, StreamBuffer, SessionStore)
from hello_agents.agents.simple_agent import SimpleAgent

class FakeLLM:                       # 假 LLM：不联网
    provider = "fake"
    def invoke(self, messages, **kw): return f"收到{len(messages)}条"

# 1) LLMResponse：__str__ 即 content
r = LLMResponse(content="hi", model="m", usage={"total_tokens": 3}, latency_ms=5)
assert str(r) == "hi" and r.to_dict()["model"] == "m"

# 2) 生命周期：构造时传 on_event，arun 前后会发 START/FINISH
events = []
async def hook(ev): events.append(ev.type)
ag = SimpleAgent(name="a", llm=FakeLLM(), on_event=hook)
assert asyncio.run(ag.arun("yo")) == "收到2条"
assert events == [EventType.AGENT_START, EventType.AGENT_FINISH]

# 3) 流式：SSE 格式 + 背压缓冲
ev = StreamEvent.create(StreamEventType.LLM_CHUNK, "a", text="x")
assert "event: llm_chunk" in ev.to_sse()
buf = StreamBuffer(max_buffer_size=2)
for i in range(3): buf.add(StreamEvent.create(StreamEventType.LLM_CHUNK, "a", i=i))
assert len(buf.get_all()) == 2          # 超容量丢最旧

# 4) 执行上下文
ctx = ExecutionContext(input_text="q"); ctx.increment_step(); ctx.add_tokens(10)
assert ctx.current_step == 1 and ctx.total_tokens == 10

# 5) 会话持久化往返
d = tempfile.mkdtemp(); ss = SessionStore(session_dir=d)
fp = ss.save(agent_config={"model": "m"}, history=[{"role": "user", "content": "hi"}],
             tool_schema_hash="abc", read_cache={}, metadata={"steps": 1}, session_name="s1")
assert ss.load(fp)["history"][0]["content"] == "hi" and ss.list_sessions()
print("✅ 核心层进阶全部通过")
```
> 这些断言全部离线、可重复，不依赖任何模型或网络——和「第 0 层用假 LLM 测逻辑」一脉相承。

---

# 第四层 · 工具系统

## 4.1 `tools/base.py` — `Tool(ABC)` + `ToolParameter`

```python
"""工具基类"""
from abc import ABC, abstractmethod
from typing import Dict, Any, List
from pydantic import BaseModel

class ToolParameter(BaseModel):
    name: str
    type: str            # "string"/"number"/"integer"/"boolean"/"array"/"object"
    description: str
    required: bool = True
    default: Any = None

class Tool(ABC):
    def __init__(self, name: str, description: str):
        self.name = name
        self.description = description

    @abstractmethod
    def run(self, parameters: Dict[str, Any]) -> str:
        """执行工具，入参是 dict，返回字符串结果"""
        ...

    @abstractmethod
    def get_parameters(self) -> List[ToolParameter]:
        """声明这个工具接受哪些参数"""
        ...

    def to_dict(self) -> Dict[str, Any]:
        return {"name": self.name, "description": self.description,
                "parameters": [p.dict() for p in self.get_parameters()]}
```
> 对照原码 `tools/base.py`（312 行）。原码还有 `@tool_action` 装饰器、`AutoGeneratedTool`(可展开子工具)、`to_openai_schema()`。**这些是给 Memory/MCP/FunctionCall 用的进阶能力，第七章核心先不写**，只保留 `run` + `get_parameters` 两个抽象方法即可。理解了主流程再回来补 `to_openai_schema()`（FunctionCallAgent 要用）。

## 4.2 `tools/registry.py` — `ToolRegistry`

工具的注册、管理、执行中心。支持两种注册：Tool 对象 / 直接注册函数。

### 必须实现
```python
class ToolRegistry:
    def __init__(self):
        self._tools: dict[str, Tool] = {}
        self._functions: dict[str, dict] = {}

    def register_tool(self, tool: Tool): ...                       # 存 Tool 对象
    def register_function(self, name, description, func): ...      # 存 func(str)->str
    def get_tool(self, name) -> Tool | None: ...
    def execute_tool(self, name: str, input_text: str) -> str: ... # ★统一执行入口
    def get_tools_description(self) -> str: ...                    # ★生成喂给提示词的工具清单
    def list_tools(self) -> list[str]: ...
    def get_all_tools(self) -> list[Tool]: ...
```

### 两个关键方法的实现
```python
def execute_tool(self, name, input_text):
    if name in self._tools:
        try:    return self._tools[name].run({"input": input_text})   # Tool 对象：包成 {"input": ...}
        except Exception as e: return f"错误：执行工具 '{name}' 时发生异常: {e}"
    elif name in self._functions:
        try:    return self._functions[name]["func"](input_text)       # 函数工具：直接调
        except Exception as e: return f"错误：执行工具 '{name}' 时发生异常: {e}"
    return f"错误：未找到名为 '{name}' 的工具。"

def get_tools_description(self):
    lines = [f"- {t.name}: {t.description}" for t in self._tools.values()]
    lines += [f"- {n}: {i['description']}" for n, i in self._functions.items()]
    return "\n".join(lines) if lines else "暂无可用工具"
```
文件末尾建一个全局实例：`global_registry = ToolRegistry()`。
> 对照原码 `tools/registry.py`（150 行）。原码的 `register_tool` 还有 `auto_expand` 展开逻辑（可展开工具），第七章先忽略。

## 4.3 `tools/builtin/calculator.py` — `CalculatorTool`

用 `ast` 安全求值的纯本地计算器（无需联网，是最稳的工具调用演示）。

```python
import ast, operator, math
from typing import Dict, Any
from ..base import Tool, ToolParameter

class CalculatorTool(Tool):
    OPERATORS = {ast.Add: operator.add, ast.Sub: operator.sub, ast.Mult: operator.mul,
                 ast.Div: operator.truediv, ast.Pow: operator.pow, ast.USub: operator.neg}
    FUNCTIONS = {'abs': abs, 'round': round, 'max': max, 'min': min, 'sum': sum,
                 'sqrt': math.sqrt, 'sin': math.sin, 'cos': math.cos, 'tan': math.tan,
                 'log': math.log, 'exp': math.exp, 'pi': math.pi, 'e': math.e}

    def __init__(self):
        super().__init__(name="python_calculator",
                         description="执行数学计算。支持基本运算、数学函数等。例如：2+3*4, sqrt(16), sin(pi/2)。")

    def run(self, parameters: Dict[str, Any]) -> str:
        expr = parameters.get("input", "") or parameters.get("expression", "")
        if not expr: return "错误：计算表达式不能为空"
        try:
            return str(self._eval(ast.parse(expr, mode='eval').body))
        except Exception as e:
            return f"计算失败: {e}"

    def _eval(self, node):
        if isinstance(node, ast.Constant): return node.value
        if isinstance(node, ast.BinOp):    return self.OPERATORS[type(node.op)](self._eval(node.left), self._eval(node.right))
        if isinstance(node, ast.UnaryOp):  return self.OPERATORS[type(node.op)](self._eval(node.operand))
        if isinstance(node, ast.Call):     return self.FUNCTIONS[node.func.id](*[self._eval(a) for a in node.args])
        if isinstance(node, ast.Name):     return self.FUNCTIONS[node.id]
        raise ValueError(f"不支持的表达式: {type(node)}")

    def get_parameters(self):
        return [ToolParameter(name="input", type="string", description="要计算的数学表达式", required=True)]

# 便捷函数：注册到 registry 时用 register_function(name, desc, calculate)
def calculate(expression: str) -> str:
    return CalculatorTool().run({"input": expression})
```
> 对照原码 `tools/builtin/calculator.py`（128 行）。

## 4.4 `tools/builtin/search_tool.py` — `SearchTool`（建议先写极简版）

原码 630 行支持 Tavily/SerpApi/DuckDuckGo/SearXNG/Perplexity 多后端，**对学习主流程是干扰**。建议先写一个极简占位版，跑通 ReAct 主流程：

```python
import os
from typing import Dict, Any, List
from ..base import Tool, ToolParameter

class SearchTool(Tool):
    def __init__(self):
        super().__init__(name="search", description="网页搜索引擎，用于查时事/事实/最新信息。")

    def run(self, parameters: Dict[str, Any]) -> str:
        query = (parameters.get("input") or parameters.get("query") or "").strip()
        if not query: return "错误：搜索查询不能为空"
        # TODO: 接 Tavily/SerpApi。没 key 时先返回提示，ReAct 循环仍能优雅结束
        if not os.getenv("TAVILY_API_KEY"):
            return f"（搜索未配置 TAVILY_API_KEY，无法联网搜索：{query}）"
        from tavily import TavilyClient
        resp = TavilyClient(api_key=os.getenv("TAVILY_API_KEY")).search(query=query, max_results=5)
        return resp.get("answer") or "\n".join(r["title"] + ": " + r["url"] for r in resp.get("results", []))

    def get_parameters(self) -> List[ToolParameter]:
        return [ToolParameter(name="input", type="string", description="搜索查询关键词", required=True)]

def search(query: str) -> str:
    return SearchTool().run({"input": query})
```
> 完整多后端版见原码 `tools/builtin/search_tool.py`。**等核心跑通后**想练，再把 Tavily/DuckDuckGo 分支补上（`pip install tavily-python ddgs`）。

### ✅ 工具层自测 `t2_tools.py`
```python
from hello_agents.tools.registry import ToolRegistry
from hello_agents.tools.builtin.calculator import calculate

reg = ToolRegistry()
reg.register_function("calculate", "数学计算", calculate)
print(reg.get_tools_description())
print(reg.execute_tool("calculate", "15*23+45"))   # 应输出 390
```

---

# 第四层 · Agent 范式（核心收尾）

## 5.1 `agents/simple_agent.py` — `SimpleAgent`

最朴素：system + history + user 一次问答；可选挂工具（文本协议 `[TOOL_CALL:name:params]`）。

### 最小实现（先不做工具，跑通对话）
```python
from typing import Optional
from ..core.agent import Agent
from ..core.message import Message

class SimpleAgent(Agent):
    def run(self, input_text: str, **kwargs) -> str:
        messages = [{"role": "system", "content": self.system_prompt or "你是一个有用的AI助手。"}]
        for msg in self._history:
            messages.append({"role": msg.role, "content": msg.content})
        messages.append({"role": "user", "content": input_text})

        response = self.llm.invoke(messages, **kwargs)
        self.add_message(Message(input_text, "user"))
        self.add_message(Message(response, "assistant"))
        return response
```
> 跑通后再看原码 `simple_agent.py`（392 行）里的工具增强：`_get_enhanced_system_prompt()`（把工具清单写进 system）、`_parse_tool_calls()`（正则 `\[TOOL_CALL:([^:]+):([^\]]+)\]`）、`run()` 里的 `while current_iteration < max_tool_iterations` 多轮工具循环。这是「文本协议工具调用」，比 ReAct 更松散，可作为进阶。

## 5.2 `agents/react_agent.py` — `ReActAgent` ★最值得手写的一个

**这是第七章的灵魂**。Thought（想）→ Action（调工具）→ Observation（看结果）→ … → Finish（给答案）。

### 提示词模板（类顶部常量）
```python
DEFAULT_REACT_PROMPT = """你是一个具备推理和行动能力的AI助手。...

## 可用工具
{tools}

## 工作流程
Thought: 分析问题...
Action: 选择工具，格式：
- `{{tool_name}}[{{tool_input}}]`：调用工具
- `Finish[结论]`：信息足够时给出最终答案

## 当前任务
**Question:** {question}

## 执行历史
{history}

现在开始你的推理和行动："""
```
> 注意模板里 `{{tool_name}}` 是转义的花括号（因为用 `str.format`），而 `{tools}/{question}/{history}` 是真正要替换的占位符。

### run() 的循环骨架（核心！）
```python
def run(self, input_text: str, **kwargs) -> str:
    self.current_history = []
    step = 0
    while step < self.max_steps:
        step += 1
        # 1. 拼提示词（工具清单由注册表给）
        prompt = self.prompt_template.format(
            tools=self.tool_registry.get_tools_description(),
            question=input_text,
            history="\n".join(self.current_history))
        # 2. 问 LLM
        text = self.llm.invoke([{"role": "user", "content": prompt}], **kwargs)
        # 3. 抽 Thought / Action
        thought, action = self._parse_output(text)
        if not action: break
        # 4. Finish？收尾返回
        if action.startswith("Finish"):
            answer = self._parse_action_input(action)
            self.add_message(Message(input_text, "user"))
            self.add_message(Message(answer, "assistant"))
            return answer
        # 5. 否则解析 工具名[参数] 并执行，把 Observation 喂回历史
        tool_name, tool_input = self._parse_action(action)
        if not tool_name:
            self.current_history.append("Observation: 无效的Action格式")
            continue
        observation = self.tool_registry.execute_tool(tool_name, tool_input)
        self.current_history.append(f"Action: {action}")
        self.current_history.append(f"Observation: {observation}")
    # 超步数兜底
    answer = "抱歉，我无法在限定步数内完成这个任务。"
    self.add_message(Message(input_text, "user")); self.add_message(Message(answer, "assistant"))
    return answer
```

### 三个正则解析（原码 202-222 行，照抄即可）
```python
import re
def _parse_output(self, text):                    # 抽 Thought / Action
    t = re.search(r"Thought: (.*)", text)
    a = re.search(r"Action: (.*)", text)
    return (t.group(1).strip() if t else None), (a.group(1).strip() if a else None)

def _parse_action(self, action_text):             # 抽 工具名[参数]
    m = re.match(r"(\w+)\[(.*)\]", action_text)
    return (m.group(1), m.group(2)) if m else (None, None)

def _parse_action_input(self, action_text):       # 抽 Finish[这里]
    m = re.match(r"\w+\[(.*)\]", action_text)
    return m.group(1) if m else ""
```

构造函数：`__init__(self, name, llm, tool_registry=None, system_prompt=None, config=None, max_steps=5, custom_prompt=None)`；`tool_registry` 为空时 `self.tool_registry = ToolRegistry()`；`self.prompt_template = custom_prompt or DEFAULT_REACT_PROMPT`。

> **读懂这段就读懂了 ReAct**：模型负责"想"，工具负责"做"，观察结果再喂回模型，循环到 `Finish`。数据流：`run()` → `get_tools_description()` 写进提示词 → 模型输出 `calculate[15*23+45]` → `_parse_action` 抽名字和参数 → `registry.execute_tool("calculate","15*23+45")` → 返回字符串当 Observation。对照原码 `react_agent.py`（222 行）。

## 5.3 `agents/reflection_agent.py` — `ReflectionAgent`

生成 → 自我反思 → 改进，多轮迭代，自带 `Memory` 记录每轮轨迹。

- 先实现一个轻量 `Memory` 类：`add_record(type, content)` / `get_last_execution()`（`type` 取 `"execution"`/`"reflection"`）。
- 三个提示词模板：`initial`（首次回答）、`reflect`（找问题，满意则回"无需改进"）、`refine`（按反馈改）。
- `run()` 流程：初始执行 → `for i in range(max_iterations)`：反思 → 若反馈含"无需改进"则 break → 否则优化 → 记录。最后返回 `memory.get_last_execution()`。
- 构造：`__init__(..., max_iterations=3, custom_prompts=None)`。

> 对照原码 `reflection_agent.py`（180 行）。适合写作/代码这类要打磨质量的任务。

## 5.4 `agents/plan_solve_agent.py` — `PlanAndSolveAgent`

先 Planner 拆计划，再 Executor 逐步执行（内含两个子角色类）。

- `Planner.plan(question)`：让 LLM 输出 ```python [...]``` 形式的步骤列表，用 `response.split("```python")[1].split("```")[0]` + `ast.literal_eval` 解析成 `list[str]`。
- `Executor.execute(question, plan)`：`for step in plan` 逐步调 LLM，把"历史步骤与结果"累积进 prompt，返回最后一步结果。
- `run()`：`plan = planner.plan(...)`；空则返回失败信息；否则 `executor.execute(...)`。
- 构造：`__init__(..., custom_prompts=None)`，`custom_prompts` 含 `"planner"`/`"executor"` 两个键。

> 对照原码 `plan_solve_agent.py`（201 行）。适合多步骤复杂任务。

## 5.5 `agents/function_call_agent.py` — `FunctionCallAgent`（进阶，可选）

用模型**原生 function calling**（JSON 工具调用）而非文本协议。需要先在 `tools/base.py` 实现 `to_openai_schema()`，再把工具 schema 通过 `tools=[...]`、`tool_choice="auto"` 传给 `chat.completions.create`，解析 `response.choices[0].message.tool_calls`，执行后把结果以 `role="tool"` 追加回 messages 再问一轮。模型支持 tools 时比文本协议更稳。

> 对照原码 `function_call_agent.py`（379 行）。建议放在最后，前面都通了再挑战。

---

## 6. `hello_agents/__init__.py` — 统一导出

让 `from hello_agents import HelloAgentsLLM, SimpleAgent, ...` 能用：
```python
from .core.llm import HelloAgentsLLM
from .core.config import Config
from .core.message import Message
from .core.exceptions import HelloAgentsException
from .agents.simple_agent import SimpleAgent
from .agents.react_agent import ReActAgent
from .agents.reflection_agent import ReflectionAgent
from .agents.plan_solve_agent import PlanAndSolveAgent
from .tools.registry import ToolRegistry, global_registry
from .tools.builtin.calculator import CalculatorTool, calculate
from .tools.builtin.search_tool import SearchTool, search

__all__ = ["HelloAgentsLLM","Config","Message","HelloAgentsException",
           "SimpleAgent","ReActAgent","ReflectionAgent","PlanAndSolveAgent",
           "ToolRegistry","global_registry","CalculatorTool","calculate","SearchTool","search"]
```
> 对照原码 `hello_agents/__init__.py`（70 行）。`core/__init__.py`、`agents/__init__.py` 等子包的 `__init__.py` 可留空或也做相应导出。

---

## 7. 验收：用官方 examples 当"集成测试"

把原版 example 复制到你的项目里跑（import 路径不变，因为包名同为 `hello_agents`）：

```bash
cp ~/repos/Fucking/Hello-Agents/examples/chapter07_react_tool_demo.py .
python chapter07_react_tool_demo.py --calc      # 只跑计算器，不需要搜索 key
```
看到 ReAct 循环打印 `🤔 思考 / 🎬 行动 / 👀 观察 / 🎉 最终答案: 390` 就说明①②③④全通了。

再跑大全套（4 种 Agent + 工具链）：
```bash
cp ~/repos/Fucking/Hello-Agents/examples/chapter07_basic_setup.py .
python chapter07_basic_setup.py
```
> 注意：`chapter07_basic_setup.py` 末尾还演示了 `ToolChain`/`AsyncToolExecutor`（原码 `tools/chain.py`、`tools/async_executor.py`），属于进阶。如果你没重写这两个，把 `demo_advanced_features()` 那段注释掉即可，不影响四种 Agent 的验证。

---

## 8. 最小可行路线（只想最快走一遍主流程）

只写这 7 个文件就能跑通一个会用工具的 ReAct Agent：
```
exceptions.py → llm.py(极简版) → message.py → config.py → agent.py
→ tools/base.py + tools/registry.py + calculator.py → react_agent.py
```
然后 `python chapter07_react_tool_demo.py --calc`。其余范式（Simple/Reflection/PlanSolve/FunctionCall）和 search、memory/rag/rl 模块，理解主流程后再按需补。

---

## 9. 常见坑

1. **ReAct 提示词的花括号**：`{{tool_name}}` 必须双写，否则 `str.format` 会报 `KeyError`。
2. **`execute_tool` 的参数包装**：Tool 对象走 `tool.run({"input": input_text})`，函数工具走 `func(input_text)` —— 别搞混。
3. **`register_function` 的 name**：必须和你希望模型在 `Action` 里写的名字一致（如注册名 `calculate`，模型才会写 `calculate[...]`）。
4. **pydantic v2**：`model.dict()` 在 v2 会有 deprecation 警告，可改 `model_dump()`；原码用的是 `.dict()`，两者都能跑。
5. **没有 LLM key**：第一层就过不了。先用 Ollama 本地模型兜底。
6. **流式 `think` 里 `chunk.choices[0].delta.content` 可能为 `None`**：记得 `or ""`。

---

## 10. 进阶：扩展模块怎么接上来

走通第七章后，`Hello-Agents/examples/` 里还有 `chapter08_memory_rag.py` / `chapter09_context_engineering.py` / `chapter10_protocols.py` / `chapter11_RL.py` 等。它们**全都建立在第七章这套核心之上**，遵循同一条铁律——**「除了 Agent，一切皆 Tool」**：

- 记忆系统 → 包成 `MemoryTool`
- RAG 检索 → 包成 `RAGTool`
- MCP/A2A/ANP 协议 → 包成 `MCPTool` / `A2ATool` / `ANPTool`
- RL 训练入口 → 包成 `RLTrainingTool`

所以扩展章节的套路是固定的：**先写底层能力模块（`memory/`、`protocols/` …）→ 再写一个 `tools/builtin/xxx_tool.py` 把它包成 `Tool` → `register_tool` 挂到 ReActAgent / SimpleAgent 上**。下面按章逐个拆。先把本指南前 9 节的四层吃透，再逐章推进。

---

# 第八章 · 记忆与检索（Memory & RAG）

> 让 Agent「记得住、查得到」。对照原码 `hello_agents/memory/`（核心层 + 类型层 + 存储层 + RAG），整合层在 `tools/builtin/memory_tool.py` 与 `rag_tool.py`。
> 对应示例：`examples/chapter08_memory_rag.py`（879 行）。

## 8.0 这一章的四层结构（先记住）

记忆系统自己也分四层，和第七章那套通信/契约/抽象/范式是**平行的扩展**：

```
Integration Layer  集成层   MemoryTool / RAGTool        ← 包成 Tool，挂给 Agent
        │
Memory Types Layer 类型层   Working/Episodic/Semantic/Perceptual ← 4 种记忆
        │
Memory Core Layer  核心层   MemoryManager               ← 统一调度 + 自动分类
        │
Storage Layer      存储层   DocumentStore / Qdrant / Neo4j        ← 落盘 / 向量 / 图
```

口诀：**Manager 是大脑，4 种 Memory 是分区，Storage 是硬盘，Tool 是插头。** 极简版只做「Working + SQLite + MemoryTool」就能跑通；完整版才上 Qdrant 向量库 / Neo4j 图库 / 多模态。

## 8.1 文件清单（建议实现顺序）

| 顺序 | 文件 | 你要实现的核心类/函数 | 依赖谁 |
|---|---|---|---|
| 1 | `memory/base.py`（170 行） | `MemoryItem`、`MemoryConfig`、`BaseMemory(ABC)` | pydantic |
| 2 | `memory/embedding.py`（315 行） | `EmbeddingModel`、`TFIDFEmbedding`、`create_embedding_model_with_fallback` | (sklearn / transformers，可降级) |
| 3 | `memory/types/working.py`（411 行） | `WorkingMemory(BaseMemory)` ★先做这个 | base |
| 4 | `memory/types/episodic.py`（598 行） | `EpisodicMemory` + `Episode` | base |
| 5 | `memory/types/semantic.py`（1182 行） | `SemanticMemory` + `Entity` + `Relation` | base、(neo4j) |
| 6 | `memory/types/perceptual.py`（709 行） | `PerceptualMemory` + `Perception`（多模态） | base |
| 7 | `memory/storage/document_store.py`（456 行） | `DocumentStore`、`SQLiteDocumentStore` | sqlite3 |
| 8 | `memory/storage/qdrant_store.py`（559 行） | `QdrantVectorStore`（可选） | qdrant-client |
| 9 | `memory/storage/neo4j_store.py`（455 行） | `Neo4jGraphStore`（可选） | neo4j |
| 10 | `memory/manager.py`（342 行） | `MemoryManager` ★核心调度 | 全部 types |
| 11 | `memory/rag/document.py`（258 行） | `Document`、`DocumentChunk`、`DocumentProcessor` | 无 |
| 12 | `memory/rag/pipeline.py`（1206 行） | RAG 流水线（切块→嵌入→索引→检索→重排） | embedding、storage |
| 13 | `tools/builtin/memory_tool.py`（544 行） | `MemoryTool(Tool)` | manager、tools/base |
| 14 | `tools/builtin/rag_tool.py`（962 行） | `RAGTool(Tool)` | rag、tools/base |

## 8.2 核心层：`memory/base.py`

三个基石，全在这一个文件里：

```python
class MemoryItem(BaseModel):              # 记忆的最小单元（pydantic）
    id: str
    content: str
    memory_type: str                      # working / episodic / semantic / perceptual
    user_id: str
    timestamp: datetime
    importance: float = 0.5               # 0~1，遗忘/整合时按它排序
    metadata: Dict[str, Any] = {}

class MemoryConfig(BaseModel):            # 各类记忆的容量/路径/阈值开关
    storage_path: str = "./memory_data"
    ...

class BaseMemory(ABC):                    # 4 种记忆的共同抽象，共 7 个抽象方法
    @abstractmethod
    def add(self, memory_item: MemoryItem) -> str: ...
    @abstractmethod
    def retrieve(self, query: str, limit: int = 5, **kwargs) -> List[MemoryItem]: ...
    @abstractmethod
    def update(self, memory_id: str, content: str = None, **kwargs) -> bool: ...
    @abstractmethod
    def remove(self, memory_id: str) -> bool: ...
    @abstractmethod
    def has_memory(self, memory_id: str) -> bool: ...      # 别漏：不实现会 TypeError
    @abstractmethod
    def clear(self): ...
    @abstractmethod
    def get_stats(self) -> Dict[str, Any]: ...             # 别漏：__str__ 依赖它

    # 下面两个是已实现的公共工具方法（子类直接用，不用重写）
    def _generate_id(self) -> str: ...                     # uuid4
    def _calculate_importance(self, content, base_importance=0.5) -> float: ...
```

**重写要点**：`BaseMemory` 和第七章的 `Tool(ABC)`、`Agent(ABC)` 思路一模一样——先定抽象契约，4 种记忆各自实现。注意是 **7 个抽象方法**（很多人只记得 add/retrieve/update/remove/clear 这 5 个，漏了 `has_memory` 和 `get_stats`，子类没实现就会 `TypeError: Can't instantiate abstract class`）。`importance` 是后面「遗忘」「整合」的排序依据，别省；`_generate_id` / `_calculate_importance` 是基类已给的工具方法，子类直接复用。

## 8.3 类型层：4 种记忆（先只做 Working）

- **WorkingMemory（工作记忆）**：短期、带容量上限的最近上下文。**极简版只做它就够跑通**。内部就是个有上限的 list，`retrieve` 按关键词重叠 + 时间倒序返回。
- **EpisodicMemory（情景记忆）**：把一次次交互打包成 `Episode`（事件序列），支持按时间/相似度回放。
- **SemanticMemory（语义记忆）**：抽取 `Entity` / `Relation` 建知识图谱，完整版落到 Neo4j。**最重最复杂（1182 行），最后做或直接降级**。
- **PerceptualMemory（感知记忆）**：把图片/音频等 `Perception` 存为多模态记忆，靠扩展名或 `modality` 参数区分。

每种都继承 `BaseMemory` 实现那 5 个抽象方法。签名统一：

```python
class WorkingMemory(BaseMemory):
    def __init__(self, config: MemoryConfig, storage_backend=None): ...
    def add(self, memory_item: MemoryItem) -> str: ...
    def retrieve(self, query: str, limit: int = 5, user_id: str = None, **kwargs) -> List[MemoryItem]: ...
```

## 8.4 核心调度：`memory/manager.py` 的 `MemoryManager`

这是记忆系统的「Agent 基类」级别的角色——统一对外，内部分发到 4 种记忆：

```python
class MemoryManager:
    def add_memory(self, content, memory_type="working", importance=0.5, ...) -> str   # 58-103
    def retrieve_memories(self, query, memory_types=None, limit=5, ...) -> List[...]    # 105-150
    def update_memory(self, memory_id, content, ...) -> bool
    def remove_memory(self, memory_id) -> bool
    def forget_memories(self, strategy="importance_based", threshold=0.1, ...)          # 194-218
    def consolidate_memories(self, from_type="working", to_type="episodic", ...)        # 220-261
    def get_memory_stats(self) -> Dict
    def clear_all_memories(self) -> None
```

**重写要点**：
- `add_memory` 里如果没显式给 `memory_type`，可以做一个 `_classify_memory_type` 自动分类（关键词/规则即可），这是「智能记忆」的关键。
- **遗忘（forget）** 三种策略：`importance_based`（低于阈值删）/ `time_based`（超 `max_age_days` 删）/ `capacity_based`（超容量删旧的）。
- **整合（consolidate）** 把高于 `importance_threshold` 的工作记忆「固化」成情景/语义记忆——模拟人类睡觉时把短期记忆转长期。

## 8.5 RAG：`memory/rag/`

- `document.py`：`Document`（原始文档）→ `DocumentProcessor.process_document()` → `List[DocumentChunk]`。核心是 `_split_text`（按长度切块、`_find_split_point` 找句子边界）、`merge_chunks`、`filter_chunks`。
- `pipeline.py`（1206 行，全章最重）：把「加载→切块→嵌入→建索引→检索→（可选）cross-encoder 重排→拼证据」串成流水线。**极简版**：TF-IDF 嵌入 + 内存向量 + cosine 检索就能跑；**完整版**才上 sentence-transformers + Qdrant + 重排。

## 8.6 整合层：包成 Tool（关键一步）

记忆和 RAG 最终都通过 `Tool` 暴露给 Agent，完全复用第七章的工具系统：

```python
# tools/builtin/memory_tool.py
class MemoryTool(Tool):
    def __init__(self, user_id="default", memory_types=["working","episodic","semantic"], ...):
        super().__init__(name="memory", description="记忆工具 - 可以存储和检索对话历史、知识和经验")
        self.manager = MemoryManager(...)

    def run(self, parameters: Dict[str, Any]) -> str:
        action = parameters.get("action")            # add/search/summary/stats/update/remove/forget/consolidate/clear_all
        if action == "add":    ...
        elif action == "search": ...
        ...
    def get_parameters(self) -> List[ToolParameter]:  # action/content/query/memory_type/importance/limit/...
        ...
```

`RAGTool` 同理，`action` 取 `add_document / add_text / ask / search / stats / clear`。

挂到 Agent 上（和第七章的 calculator/search 一样）：

```python
from hello_agents import SimpleAgent, HelloAgentsLLM, ToolRegistry
from hello_agents.tools import MemoryTool, RAGTool

registry = ToolRegistry()
registry.register_tool(MemoryTool(user_id="u1", memory_types=["working","episodic","semantic"]))
registry.register_tool(RAGTool())
agent = SimpleAgent(name="记忆助手", llm=HelloAgentsLLM(), tool_registry=registry, system_prompt="...")
```

## 8.7 ✅ 自测脚本 `t3_memory.py`

```python
from hello_agents.memory import MemoryManager, MemoryConfig

mm = MemoryManager(config=MemoryConfig(storage_path="./_mem_test"))
mid = mm.add_memory("我叫小明，是后端工程师", memory_type="working", importance=0.9)
print("写入:", mid)
hits = mm.retrieve_memories("我是谁", limit=3)
print("检索:", [h.content for h in hits])
print("统计:", mm.get_memory_stats())
mm.consolidate_memories(from_type="working", to_type="episodic", importance_threshold=0.7)
print("整合后:", mm.get_memory_stats())
```

跑绿了说明核心层 + 类型层 + 存储层通了。RAG 单独测：`RAGTool().run({"action":"add_text","text":"地球绕太阳转"})` 再 `{"action":"ask","question":"地球绕什么转"}`。

## 8.8 常见坑

1. **可选依赖没装就 import 报错**：`qdrant-client` / `neo4j` / `sentence-transformers` 都是可选的。原码用 `try/except ImportError` 降级到 SQLite / TF-IDF——你重写时务必照做，别在顶层硬 import。安装：`pip install hello-agents[memory,rag]`。
2. **嵌入模型首次下载慢/无网**：`create_embedding_model_with_fallback` 会在 transformers 不可用时退回 TF-IDF。本地无网就直接用 TF-IDF。
3. **`importance` 不设导致遗忘策略失效**：`add_memory` 记得传或自动算 importance。
4. **Neo4j/Qdrant 没起服务**：完整版需要本地起容器；调试阶段用极简版（纯内存/SQLite）。

---

# 第九章 · 上下文工程（Context Engineering）

> 精细控制「到底把什么、按什么顺序、塞多少」喂给 LLM。对照原码 `hello_agents/context/builder.py`（353 行）。
> ⚠️ 注意：`examples/chapter09_context_engineering.py` 是个 23 行的占位 stub，**真正实现全在 `context/builder.py`**，别被示例误导。

## 9.0 核心思想：GSSC 四步流水线

上下文工程的本质是一条 **GSSC** 流水线：

```
Gather   收集   把候选材料全捞上来（指令/记忆/知识库/历史）
  │
Select   筛选   按相关性+新近度打分，可选 MMR 去冗余，按 token 预算选
  │
Structure 组织  按固定模板排版（角色→任务→状态→证据→历史→输出格式）
  │
Compress 压缩   还超预算就截断/摘要，保证不超 max_tokens
```

一句话：**Gather 捞、Select 挑、Structure 排、Compress 压。**

## 9.1 文件清单

| 顺序 | 文件 | 核心类/函数 | 依赖 |
|---|---|---|---|
| 1 | `context/builder.py` | `ContextPacket`、`ContextConfig`、`ContextBuilder`、`count_tokens` | tiktoken、memory_tool、rag_tool |

只有一个文件，但是浓缩。`pip install tiktoken`。

## 9.2 三个核心结构

```python
@dataclass
class ContextPacket:                    # 一块上下文候选
    content: str
    source: str                         # instruction / memory / knowledge / history
    priority: int                       # P0 最高
    relevance_score: float = 0.0
    token_count: int = 0
    metadata: dict = ...

class ContextConfig:                    # 预算与开关
    max_tokens: int = 8000
    reserve_ratio: float = 0.2          # 给输出留 20%
    min_relevance: float = 0.0
    enable_mmr: bool = False            # 最大边际相关，去冗余
    mmr_lambda: float = 0.7
    enable_compression: bool = True
    def get_available_tokens(self) -> int:   # max_tokens * (1 - reserve_ratio)
        ...

class ContextBuilder:
    def __init__(self, memory_tool=None, rag_tool=None, config=ContextConfig()): ...
    def build(self, query, instructions=None, task_state=None) -> str:
        packets = self._gather(...)     # 122-201
        packets = self._select(packets) # 203-258
        text = self._structure(packets) # 260-313
        return self._compress(text)     # 315-342
```

## 9.3 四步分别怎么写

- **`_gather`**：按优先级捞材料——P0 系统指令 / P1 任务状态 + `memory_tool.run({"action":"search",...})` 的相关记忆 / P2 `rag_tool.run({"action":"search",...})` 的知识库 / P3 对话历史。每块包成 `ContextPacket` 并用 `count_tokens` 算 token。
- **`_select`**：相关性（query 与 content 的关键词重叠 / 嵌入相似度）× 0.7 + 新近度（时间指数衰减）× 0.3 算综合分；`enable_mmr` 时用 MMR 去掉内容重复的块；按综合分排序，在 `get_available_tokens()` 预算内贪心填充。
- **`_structure`**：固定模板排版，让 LLM 好读：

```
[Role & Policies]   ← 指令
[Task]              ← 当前问题
[State]             ← 任务状态
[Evidence]          ← RAG 证据（带来源）
[Relevant Context]  ← 记忆 / 历史
[Output Format]     ← 输出要求
```

- **`_compress`**：组装完若仍超 `max_tokens`，按优先级从低往高截断或摘要。

## 9.4 ✅ 自测脚本 `t4_context.py`

```python
from hello_agents.context.builder import ContextBuilder, ContextConfig

b = ContextBuilder(config=ContextConfig(max_tokens=2000, reserve_ratio=0.25))
ctx = b.build(
    query="帮我优化这段排序代码",
    instructions="你是资深 Python 工程师，回答务必给出复杂度分析。",
    task_state="用户已贴出 bubble sort 实现",
)
print(ctx)
print("约 tokens:", b.count_tokens(ctx) if hasattr(b, "count_tokens") else "n/a")
```

看输出是否按 `[Role]/[Task]/[State]/...` 分段、且不超预算即可。

## 9.5 常见坑

1. **`tiktoken` 无网下载 BPE 文件失败**：`count_tokens` 退化为「按字符数 / 4」估算，别让它直接抛异常。
2. **`reserve_ratio` 忘了留**：不留输出预算，长回答会被模型截断。
3. **MMR 开了但没嵌入模型**：`enable_mmr=True` 依赖相似度；没嵌入时退回纯关键词去重。
4. **Builder 没传 memory_tool/rag_tool 也要能跑**：两个都 `=None` 时 `_gather` 只用 instructions/history，别 `NoneType` 崩。

---

# 第十章 · 智能体通信协议（MCP / A2A / ANP）

> 让 Agent 能调外部工具、和别的 Agent 协作、在网络里被发现。对照原码 `hello_agents/protocols/`，整合层在 `tools/builtin/protocol_tools.py`（851 行）。
> 示例：`examples/chapter10_protocols.py`（1281 行）。安装：`pip install hello-agents[protocols]`（fastmcp、a2a-sdk）。

## 10.0 三个协议各管什么

| 协议 | 全称 | 解决什么 | 类比 |
|---|---|---|---|
| **MCP** | Model Context Protocol | Agent ↔ **工具/资源** 的标准接口 | 「USB 接口」——插上就能用别人的工具 |
| **A2A** | Agent-to-Agent | Agent ↔ **Agent** 的协作 / 技能调用 | 「同事之间打电话」 |
| **ANP** | Agent Network Protocol | Agent 在**网络**里注册、发现、路由 | 「黄页 / DNS」 |

## 10.1 文件清单

| 顺序 | 文件 | 核心类/函数 | 依赖（可选） |
|---|---|---|---|
| 1 | `protocols/base.py`（63 行） | `ProtocolType`(枚举: MCP/A2A/ANP)、`Protocol` | 无 |
| 2 | `protocols/mcp/server.py`（216 行） | `MCPServer` | fastmcp |
| 3 | `protocols/mcp/client.py`（353 行） | `MCPClient` | mcp |
| 4 | `protocols/mcp/utils.py`（144 行） | `create_context`、`parse_context` | 无 |
| 5 | `protocols/a2a/implementation.py`（439 行） | `A2AServer`、`A2AClient`、`AgentNetwork`、`AgentRegistry` | a2a-sdk |
| 6 | `protocols/anp/implementation.py`（327 行） | `ServiceInfo`、`ANPDiscovery`（含 `register_service` 方法）、`ANPNetwork` | 无（概念性实现） |
| 7 | `tools/builtin/protocol_tools.py`（851 行） | `MCPTool`、`A2ATool`、`ANPTool` | 上面全部 |

## 10.2 重写策略：优雅降级是第一原则

这一章和别处不同——`fastmcp` / `mcp` / `a2a-sdk` 都是**重量级可选依赖，没装就该静默降级**，不能让 import 直接崩。原码模式（务必照抄）：

```python
try:
    from fastmcp import FastMCP
    _MCP_AVAILABLE = True
except ImportError:
    _MCP_AVAILABLE = False

class MCPServer:
    def __init__(self, ...):
        if not _MCP_AVAILABLE:
            raise HelloAgentsException("MCP 需要 fastmcp，请 pip install hello-agents[protocols]")
        ...
```

## 10.3 MCPTool：最常用，重点做

`MCPTool` 把一个 MCP 服务器包成第七章的 `Tool`，`run` 按 `action` 分发：

```python
class MCPTool(Tool):
    def __init__(self, server_command=None, server_url=None, ...):
        # 连接 MCP server（stdio 或 http/sse），自动拉取远端 tools 生成 description
        ...
    def run(self, parameters):
        action = parameters.get("action")
        # list_tools / call_tool / list_resources / read_resource / list_prompts / get_prompt
        ...
```

注意 MCP 客户端是 **async** 的，而第七章的 `Tool.run` 是同步的——原码用「在新线程里跑 event loop」(`run_in_thread` + `asyncio.run`) 把 async 桥接成 sync。重写时这个桥接别漏。

`A2ATool` / `ANPTool` 同样继承 `Tool`：A2A 负责把任务委托给远端 Agent；ANP 的 `action` 取 `register_service / unregister_service / discover_services / add_node / route_message / get_stats`（注意是复数 `discover_services`）。

## 10.4 ✅ 自测脚本 `t5_protocol.py`

```python
# 不依赖外网：先验证「没装依赖时优雅降级」
from hello_agents.protocols import ProtocolType
print("协议枚举:", [p.value for p in ProtocolType])

# 装了 fastmcp 再测真实连接（举例：连一个本地 stdio MCP server）
from hello_agents.tools import MCPTool
try:
    tool = MCPTool(server_command="python my_mcp_server.py")
    print(tool.run({"action": "list_tools"}))
except Exception as e:
    print("降级/未连接（符合预期）:", e)
```

## 10.5 常见坑

1. **async/sync 桥接**：MCP/A2A 底层 async，包进同步 `Tool.run` 时务必用独立线程跑 loop，否则和外层已有的 event loop 冲突报 `RuntimeError: ... already running`。
2. **`a2a-sdk` 版本漂移**：A2A 仍在早期（`a2a-sdk>=0.1.0`），接口不稳，重写时把它当「可选 + 占位实现」。
3. **ANP 是概念性实现**：`anp/implementation.py` 偏教学（内存里的服务注册表），别期待生产级网络栈。
4. **MCP server 路径/命令错**：`server_command` 要能在当前环境直接拉起，注意 cwd 和 Python 解释器。

---

# 第十一章 · Agentic-RL（强化学习训练）

> 训练 / 微调一个会用工具、会推理的模型。对照原码 `hello_agents/rl/`，整合层 `tools/builtin/rl_training_tool.py`。
> 示例：`examples/chapter11_RL.py`（349 行）。安装：`pip install hello-agents[rl]`（trl、transformers、torch、datasets、accelerate、peft、bitsandbytes）。

## 11.0 大图：LLM 的成长路线

```
预训练(别人做)  →  SFT(监督微调，学会"照着示范答")  →  RL(GRPO/PPO，按奖励自我优化)
                         │                                    │
                    需要 (问题, 标准答案) 数据集         需要 奖励函数 (答得对/格式好 给高分)
```

LoRA 贯穿始终——只训练少量低秩参数，省显存，让 0.6B 小模型也能在单卡跑通。

## 11.1 文件清单

| 顺序 | 文件 | 核心类/函数 | 说明 |
|---|---|---|---|
| 1 | `rl/utils.py`（225 行） | `TrainingConfig`、`setup_training_environment`、`check_trl_installation`、`get_installation_guide` | 配置 + 环境检查 |
| 2 | `rl/datasets.py`（369 行） | `GSM8KDataset`、`create_math_dataset`、`create_sft_dataset`、`create_rl_dataset`、`preview_dataset` | 数据 |
| 3 | `rl/rewards.py`（268 行） | `MathRewardFunction`、`create_accuracy_reward`、`create_length_penalty_reward`、`evaluate_rewards` | 奖励 |
| 4 | `rl/trainers.py`（412 行） | `BaseTrainerWrapper`、`SFTTrainerWrapper`、`GRPOTrainerWrapper`、`PPOTrainerWrapper` | 训练器 |
| 5 | `tools/builtin/rl_training_tool.py` | `RLTrainingTool(Tool)` | 整合层 |

## 11.2 配置中心：`TrainingConfig`（dataclass）

所有训练共用一个配置对象（默认值就是教学友好的小模型配置）：

```python
@dataclass
class TrainingConfig:
    model_name: str = "Qwen/Qwen3-0.6B"   # 默认小模型，2-3 分钟能跑完
    output_dir: str = "./output"
    num_train_epochs: int = 3
    per_device_train_batch_size: int = 4
    gradient_accumulation_steps: int = 4
    learning_rate: float = 5e-5
    # LoRA
    use_lora: bool = True
    lora_r: int = 16
    lora_alpha: int = 32
    lora_target_modules: list = field(default_factory=lambda: ["q_proj", "v_proj"])  # 别写成裸 list，dataclass 会报 mutable default
    # 监控
    use_wandb: bool = False
    use_tensorboard: bool = True
    seed: int = 42
```

## 11.3 三个训练器：统一 `.train()`

```python
class BaseTrainerWrapper:
    def __init__(self, config: TrainingConfig = None): ...
    def train(self): ...                  # 子类各自实现

class SFTTrainerWrapper(BaseTrainerWrapper):   # 监督微调（先做，最成熟）
class GRPOTrainerWrapper(BaseTrainerWrapper):  # Group Relative Policy Optimization（本章重点，有完整实现）
class PPOTrainerWrapper(BaseTrainerWrapper):   # 占位：原码里 train() 直接 raise NotImplementedError
```

> 注意：`PPOTrainerWrapper` 是**占位实现**，调用 `.train()` 会抛 `NotImplementedError("PPO训练器尚未实现，请使用GRPOTrainerWrapper")`。要跑 RL 请用 `GRPOTrainerWrapper`（作者有意取舍，GRPO 才是这一版的主路）。

它们底层包的是 HuggingFace **TRL** 库的 `SFTTrainer` / `GRPOTrainer` / `PPOTrainer`，外面套 LoRA（peft）和我们的 `TrainingConfig`。**重写要点**：用 `TRL_AVAILABLE` flag + `check_trl_installation()` 在没装 trl 时给出 `get_installation_guide()` 的友好提示，而不是 ImportError 崩掉。

## 11.4 奖励函数：`rewards.py`

RL 阶段的灵魂——告诉模型「什么样的回答好」：

```python
class MathRewardFunction:                 # 数学题：答案对不对（抽取最终数字比对）
    def __call__(self, completions, **kwargs) -> List[float]: ...

create_accuracy_reward()        # 准确性奖励
create_length_penalty_reward()  # 太长扣分
create_step_reward()            # 有清晰推理步骤加分
evaluate_rewards(reward_fn, samples)   # 离线检查奖励函数是否合理
```

## 11.5 整合层 + 一键流程

`RLTrainingTool` 把整套流程包成 Tool，`action` 取 `train / load_dataset / create_reward / evaluate`：

```python
from hello_agents.tools import RLTrainingTool
tool = RLTrainingTool()
tool.run({"action": "load_dataset", "dataset": "gsm8k", "num_samples": 10})
tool.run({"action": "train", "method": "sft", "model_name": "Qwen/Qwen3-0.6B", "num_epochs": 1})
tool.run({"action": "train", "method": "grpo", "num_epochs": 1})
tool.run({"action": "evaluate"})
```

## 11.6 ✅ 自测脚本 `t6_rl.py`

```python
from hello_agents.rl import TrainingConfig, preview_dataset, create_math_dataset
# 注意：check_trl_installation / get_installation_guide 没在 rl/__init__ 里导出，要从子模块 utils 导
from hello_agents.rl.utils import check_trl_installation, get_installation_guide

if not check_trl_installation():
    print(get_installation_guide()); raise SystemExit
ds = create_math_dataset(max_samples=5)   # 注意参数名是 max_samples，不是 num_samples
preview_dataset(ds)                       # preview_dataset 才是 num_samples=3
print("配置:", TrainingConfig(num_train_epochs=1).to_dict())
```

跑得通说明数据/配置/环境检查 OK，再去跑真正的 `SFTTrainerWrapper(...).train()`（要 GPU，CPU 也能跑小模型但慢）。

## 11.7 常见坑

1. **没 GPU / 显存不足**：用默认 `Qwen/Qwen3-0.6B` + `use_lora=True` + 小 batch；`bitsandbytes` 量化进一步省显存（但 Windows 上 `bitsandbytes` 装起来麻烦，可不开量化）。
2. **trl 版本 API 变动大**：原码锁 `trl>=0.24.0`，TRL 的 `GRPOTrainer` 接口在多个版本间改过名，重写时以装到的版本为准。
3. **数据集联网下载失败**：`create_math_dataset` 底层是 `GSM8KDataset`，会 `load_dataset("openai/gsm8k", "main")` 从 HuggingFace 拉数据——**并没有本地合成降级**。无网环境要么先把 HF 数据集缓存好（设 `HF_DATASETS_CACHE` / 镜像 `HF_ENDPOINT`），要么跳过这一步。
4. **奖励函数返回长度要对齐 completions**：`__call__` 必须返回和输入 batch 等长的 `List[float]`，否则 TRL 报错。

---

# 第十二章 · 智能体性能评估（Evaluation）

> 量化「这个 Agent 到底行不行」。对照原码 `hello_agents/evaluation/`。安装：`pip install hello-agents[evaluation]`。

## 12.0 两个标准基准 + 数据质量评估

| 基准 | 全称 | 测什么 |
|---|---|---|
| **BFCL** | Berkeley Function Calling Leaderboard | Agent **调工具/函数**调得准不准 |
| **GAIA** | General AI Assistants | 综合**通用助手**能力（多步推理 + 工具 + 检索） |

外加 **数据生成质量评估**（`data_generation/`）：用 `LLMJudgeEvaluator`（让强模型当裁判打分）和 `WinRateEvaluator`（两个回答两两对比算胜率）。

## 12.1 文件清单

| 模块 | 文件 | 核心类 |
|---|---|---|
| BFCL | `evaluation/benchmarks/bfcl/dataset.py` | `BFCLDataset` |
| BFCL | `evaluation/benchmarks/bfcl/evaluator.py` | `BFCLEvaluator` |
| GAIA | `evaluation/benchmarks/gaia/dataset.py` | `GAIADataset` |
| GAIA | `evaluation/benchmarks/gaia/evaluator.py` | `GAIAEvaluator` |
| 数据质量 | `evaluation/benchmarks/data_generation/dataset.py` | `AIDataset` |
| 数据质量 | `evaluation/benchmarks/data_generation/llm_judge.py` | `LLMJudgeEvaluator` |
| 数据质量 | `evaluation/benchmarks/data_generation/win_rate.py` | `WinRateEvaluator` |
| 整合层 | `tools/builtin/*_evaluation_tool.py` | `BFCLEvaluationTool` / `GAIAEvaluationTool` 等 |

## 12.2 用法骨架（评估器统一 `.evaluate(agent, ...)`）

```python
from hello_agents.evaluation import BFCLDataset, BFCLEvaluator

dataset = BFCLDataset(category="simple_python")
evaluator = BFCLEvaluator(dataset=dataset)
results = evaluator.evaluate(agent, max_samples=5)
print("准确率:", results["overall_accuracy"])
```

GAIA 同构：`GAIADataset` + `GAIAEvaluator`。数据质量评估：

```python
from hello_agents.evaluation import LLMJudgeEvaluator
judge = LLMJudgeEvaluator(llm=HelloAgentsLLM())
# 方法名是 evaluate_single（不是 evaluate）；入参是一个 problem 字典，不是散开的 question/answer
result = judge.evaluate_single(problem={"question": "...", "answer": "..."}, reference=None)
```

## 12.3 ✅ 自测脚本 `t7_eval.py`

```python
from hello_agents.evaluation import BFCLDataset
ds = BFCLDataset(category="simple_python")
# BFCLDataset 有 __len__/__iter__，但**没有 __getitem__**：取单条用 get_sample(i)，不能 ds[0]
print("样本数:", len(ds), "示例:", ds.get_sample(0))
# 有 agent 后：BFCLEvaluator(dataset=ds).evaluate(agent, max_samples=3)
```

## 12.4 常见坑

1. **BFCL 依赖与主依赖冲突**：`bfcl-eval` 强制 `numpy==1.26.4`，和主项目 `numpy>=2.0` 冲突，**不能** `pip install hello-agents[evaluation-bfcl]`。按 `pyproject.toml` 注释，单开一个虚拟环境装（`uv venv .venv-bfcl` → 装 `numpy==1.26.4 bfcl-eval ...`）。
2. **GAIA 数据集要 HuggingFace 鉴权**：部分 GAIA 子集 gated，需要 `huggingface-cli login`。
3. **评估要花 LLM 调用**：`max_samples` 先设小（3~5）跑通流程，再放大。
4. **LLMJudge 偏向长答案**：用 `WinRateEvaluator` 做两两对比能缓解长度偏置。

---

# 第十三~十六章 · 应用实战（Capstone）

> 这几章**不再引入新的核心模块**，而是把第七~十二章的能力组装成完整应用。重写思路：以第七章的 Agent + 工具系统为骨架，按需挂上前面几章的 Tool（Memory/RAG/MCP/...）。

| 章 | 应用 | 主要复用的能力 |
|---|---|---|
| 第十三章 | 智能旅行助手 | MCP 工具（地图/天气）+ 多智能体协作（A2A） |
| 第十四章 | 自动化深度研究（Deep Research） | RAG + 上下文工程 + ReAct/Plan-Solve |
| 第十五章 | 赛博小镇（社会模拟） | 记忆系统（情景/语义）+ 多 Agent |
| 第十六章 | 毕业设计（综合项目） | 以上全部 |

**做法**：先在 `examples/` 里跑通对应章节示例，理清它用了哪些 Tool；再照「先能力模块 → 包 Tool → 挂 Agent」的套路自己搭。这几章没有"重写一个新框架"的负担，重点是**编排（orchestration）**。具体业务逻辑参考上游教程 [datawhalechina/hello-agents](https://github.com/datawhalechina/hello-agents) 第 13~16 章。

---

祝重写顺利。卡在哪一层、报什么错，把错误贴出来即可对照原码定位。
